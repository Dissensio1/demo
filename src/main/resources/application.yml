spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/timetabbble
        username: postgres
        password: bebrabebra
        driver-class-name: org.postgresql.Driver
    jpa:
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: update
---
spring:
    profiles:
        active: dev
    application:
        name: "Demo"
        version: "0.1"
---
jwt:
    secret:
    access:
        duration:
            minutes: 5
            second: 300
        cookie-name: access_token
    refresh:
        duration:
            day: 7
            second: 604800
        cookie-name: refresh_token