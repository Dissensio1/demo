spring:
    datasource:
        url: jdbc:postgresql://localhost:5432/timetabbble
        username: postgres
        password: 45Feargta444
        driver-class-name: org.postgresql.Driver
    jpa:
        database-platform: org.hibernate.dialect.PostgreSQLDialect
        hibernate:
            ddl-auto: update
---
spring:
    profiles:
        active: dev
    application:
        name: "Demo"
        version: "0.1"